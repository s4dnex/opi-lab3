<?xml version="1.0" encoding="UTF-8"?>

<project name="opi-lab3" default="build">
    <property file="build.properties"/>

    <target name="compile" depends="clean">
        <mkdir dir="${build.classes.dir}"/>
        <javac destdir="${build.classes.dir}" includeantruntime="false">
            <classpath>
                <path>
                    <fileset dir="${backend.libs.dir}" includes="*.jar"/>
                    <fileset dir="${backend.src.java.dir}" includes="*.java"/>
                </path>
            </classpath>
            <src path="${src.dir}/main"/>
        </javac>
    </target>

    <target name="build" depends="compile">
        <war destfile="${build.dir}/${destination-name}.war" webxml="${frontend.src.dir}/WEB-INF/web.xml">
            <fileset dir="${frontend.src.dir}"/>
            <metainf dir="${backend.src.resource.dir}/META-INF"/>
            <classes dir="${build.dir}/classes"/>
            <lib dir="${backend.libs.dir}"/>
        </war>
    </target>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="test" depends="build">
        <!--TODO-->
    </target>

    <target name="xml">
        <!--TODO-->
    </target>

    <target name="music" depends="build">
        <!--TODO-->
    </target>

    <target name="doc">
        <!--TODO-->
    </target>

    <target name="native2ascii">
        <!--TODO-->
    </target>

    <target name="env">
        <!--TODO-->
    </target>

    <target name="team">
        <!--TODO-->
    </target>

    <target name="diff">
        <!--TODO-->
    </target>

    <target name="alt">
        <!--TODO-->
    </target>
</project>